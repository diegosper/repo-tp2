class MyArray {
    constructor(){
        this.elementos = []; 
        
    }

    map(callbackDeConversion) {
        const nuevoArray = [];

        for (let i = 0; i < this.elementos.length; i++) {
            const elementoActual = array[i];
            
        }
    }

    

    push(elem) {
        this.elementos.push(elem);
    }
}

module.exports = MyArray;